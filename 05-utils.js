const sayHi = (name) => {
    console.log(`Hello there ${name}`)
}


module.exports = sayHi
// como solo comparto un elemento no hace falta poner entre llaves, como si fuera un objeto
//veremos luego que hay otros modos de setear el export