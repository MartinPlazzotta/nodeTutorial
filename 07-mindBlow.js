const a = 10
const b = 20

function add() {
    x = a + b
    console.log(`Teh sum is ${a + b}`) 
    //usar el character ` para que reconzoca a  y b como variables 
    //si usas singleQuote para el argumento del log() entonces no reconoce a y b como numeros
}

add()